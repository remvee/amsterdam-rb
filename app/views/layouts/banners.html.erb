<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Amsterdam.rb - Banners</title>
    <style type="text/css">
      label { display: block; }
      .error_message { color: red; padding-left: 1em; }
      tr.odd { background-color: #eee; }
    </style>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
